# Mailgun Configuration
# Required: Your Mailgun Private API key
MAILGUN_API_KEY=your_mailgun_api_key_here

# Required: Your Mailgun domain (e.g., mg.yourdomain.com or sandbox domain for testing)
MAILGUN_DOMAIN=mg.yourdomain.com

# Optional: Email address to send from (defaults to newsletter@${MAILGUN_DOMAIN})
SUBSCRIBE_FROM_EMAIL=newsletter@yourdomain.com

# Optional: Email address to receive subscription notifications (defaults to subscribe@${MAILGUN_DOMAIN})
SUBSCRIBE_TO_EMAIL=subscribe@yourdomain.com
